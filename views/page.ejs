<%- include('partials/header') %>
<%- include('partials/nav') %>
<div class="container mb-3">
    <h1><%= page.title %></h1> <!-- Display the title of the page -->
    <div class="row">
    <div class="page-content text-center col-6 m-auto">
        <% if (page.img) { %>
            <img src="/<%= page.img %>" alt="Hero Image" style="width: 100%; max-width: 600px" class="m-auto"/> <!-- Display the image if it exists -->
        <% } %>
    </div>
    <div class="col-6"><%- page.tar %><div> <!-- Display the content of the page -->
    </div>
</div>

<%- include('partials/footer') %>

